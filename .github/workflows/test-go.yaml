---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Test Go

on:
  # Manual Trigger
  workflow_dispatch: {}

  # Run on any PR
  pull_request: {}

jobs:
  test:
    uses: mirceanton/reusable-workflows/.github/workflows/reusable-go-test.yaml@03898112670be8cb0d0f94d80b3c3aa6dbdc12d7 # v3.4.43
    secrets: inherit
    with:
      go-version: stable
