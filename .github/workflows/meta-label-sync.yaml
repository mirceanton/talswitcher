---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Meta - Label Sync

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Run the action without making changes"
        default: true
        required: false
        type: boolean

  pull_request:
    paths:
      - ".github/configs/labels.yaml"
      - ".github/workflows/meta-label-sync.yaml"

  push:
    branches: ["main"]
    paths:
      - ".github/configs/labels.yaml"
      - ".github/workflows/meta-label-sync.yaml"

  schedule:
    - cron: "0 0 * * *" # Every day at midnight

jobs:
  label-sync:
    uses: mirceanton/reusable-workflows/.github/workflows/meta-label-sync.yaml@5420663687d3db97f9f4bc50854259e1ce3b38f6
    secrets: inherit
    with:
      dry-run: ${{ inputs.dry-run || github.event_name == 'pull_request' }}
      config-file: ".github/configs/labels.yaml"
