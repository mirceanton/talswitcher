---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Go Release

on:
  workflow_dispatch: {}

  push:
    tags: ["*"]

  pull_request:
    paths:
      - ".github/workflows/go-release.yaml"
      - ".goreleaser.yaml"
      - "go.mod"
      - "go.sum"
      - "cmd/**"

jobs:
  goreleaser:
    uses: mirceanton/reusable-workflows/.github/workflows/go-release.yaml@5420663687d3db97f9f4bc50854259e1ce3b38f6
    secrets: inherit
    with:
      go-version: "1.22.5"
      goreleaser-version: "v2.2.0"
      dry-run: ${{ inputs.dry-run || github.event_name == 'pull_request' }}
